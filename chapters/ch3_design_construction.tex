\chapter{METHODOLOGY}

\section{System Overview}

The Satu robotic assistant is implemented as a distributed embedded architecture consisting of four computational nodes: a central AI Brain server, an NVIDIA Jetson Orin Nano vision module, and two Raspberry Pi~5 edge devices. This distributed design balances computational workload while minimising latency across perception, reasoning, and actuation subsystems.

The AI Brain server acts as the core intelligence layer, running a FastAPI-based service that orchestrates the full interaction pipeline. It integrates large language model (LLM) inference via Ollama, a Retrieval-Augmented Generation (RAG) pipeline, speech-to-text (STT) and text-to-speech (TTS) processing through the Typhoon2-Audio sidecar, and database services. A hybrid storage system is employed, combining Milvus for vector-based semantic retrieval and MySQL for structured relational data. Additionally, SQLite is used for lightweight conversation logging and system monitoring.

The Jetson Orin Nano module is responsible for real-time face detection and recognition, leveraging GPU acceleration through TensorRT-optimised ONNX models to perform biometric identification of registered users with low latency.

The first Raspberry Pi~5 functions as the audio interface and coordination node, handling microphone input, performing voice activity detection and on-device speech transcription, and playing back synthesised speech responses received from the server. It also hosts the robot's expressive face display, rendered on an HDMI screen using an Electron.js application. The second Raspberry Pi~5 operates as the mobility controller, executing ROS2-based navigation, including path planning, wheel control, and obstacle avoidance.

\begin{table}[h]
\centering
\label{tab:system_components}
\begin{tabular}{|l|l|}
\hline
\textbf{Component} & \textbf{Specification} \\ \hline
AI Brain server            & Ubuntu 22.04 LTS, NVIDIA GPU \\ \hline
Raspberry Pi~5 (audio + display) & Raspberry Pi OS (64-bit) \\ \hline
Raspberry Pi~5 (navigation) & Raspberry Pi OS (64-bit) \\ \hline
Jetson Orin Nano           & NVIDIA Jetson Orin Nano (8 GB) \\ \hline
LiDAR sensor               & RPLiDAR A1 (2D, 360°) \\ \hline
Drive controller           & iREDCr differential drive \\ \hline
Navigation middleware      & ROS2 Jazzy Jalisco + Nav2 \\ \hline
LLM inference              & Ollama (Typhoon2-70B, Q5\_K\_M GGUF) \\ \hline
Vector database            & Milvus v2.6 \\ \hline
Relational database        & MySQL + SQLite \\ \hline
Audio processing           & Typhoon2-Audio 8B (STT + TTS sidecar) \\ \hline
Face recognition           & SCRFD + ArcFace (ONNX / TensorRT) \\ \hline
\end{tabular}
\caption{System Components and Specifications}
\end{table}

\subsection{Distributed Node Architecture}

The four computational nodes are connected over a local area network and communicate via a combination of persistent WebSocket channels and stateless HTTP requests. The network topology is intentionally hub-and-spoke: the AI Brain server acts as the central coordinator, with all edge nodes communicating exclusively with the server rather than with one another directly, with the single exception of the Jetson-to-Pi~5 WebSocket link used for low-latency face detection events.

\begin{table}[h]
\centering
\label{tab:network_endpoints}
\begin{tabular}{|l|l|l|l|l|}
\hline
\textbf{Host} & \textbf{IP Address} & \textbf{Port} & \textbf{Protocol} & \textbf{Service} \\ \hline
AI Brain server         & 10.100.16.22 & 8000  & HTTP      & Main FastAPI service \\ \hline
AI Brain server         & 10.100.16.22 & 8001  & HTTP      & Typhoon2-Audio sidecar \\ \hline
AI Brain server         & 10.100.16.22 & 19530 & gRPC      & Milvus vector database \\ \hline
Jetson Orin Nano        & 192.168.1.10 & 9999  & TCP       & Remote shutdown listener \\ \hline
Pi~5 \#1 (audio)       & 10.26.9.196  & 8765  & WebSocket & Jetson detection receiver \\ \hline
Pi~5 \#1 (audio)       & 10.26.9.196  & 8766  & HTTP      & RaspiReceive FastAPI \\ \hline
Pi~5 \#1 (display)     & 10.26.9.196  & 7000  & HTTP      & Face emotion API \\ \hline
Pi~5 \#1 (internal)    & 127.0.0.1    & 8768  & WebSocket & Face UI IPC bridge \\ \hline
Pi~5 \#2 (navigation)  & 10.26.9.196  & 8767  & HTTP      & ROS2 navigation bridge \\ \hline
\end{tabular}
\caption{Network Endpoints and Communication Protocols}
\end{table}

\subsection{End-to-End Interaction Pipeline}

The complete pipeline for a single human-robot interaction follows a linear sequence of processing stages:

\begin{enumerate}
    \item The Jetson Orin Nano continuously captures camera frames and applies face detection and recognition. Upon identifying a student or an unidentified visitor, it transmits a detection event as a JSON message over the persistent WebSocket to Pi~5 \#1.

    \item Pi~5 \#1 receives the detection event and simultaneously records microphone audio. A Voice Activity Detection model gates speech capture, and a speech recognition model transcribes the captured segment.

    \item The coordination logic on Pi~5 \#1 combines the detection event with the transcription result and dispatches a single JSON payload to the AI Brain server via an HTTP POST request.

    \item The AI Brain server executes the full inference pipeline: grammar correction, query routing, RAG retrieval, LLM response generation, and TTS synthesis.

    \item The server dispatches responses asynchronously: synthesised audio (WAV) to Pi~5 \#1 for playback, a microphone activation flag (\textbf{/set\_active}) to the audio controller, and when required, a navigation command to Pi~5 \#2. Face emotion transitions are managed locally by Pi~5 \#1 based on its own audio playback lifecycle.

    \item Pi~5 \#2 translates the HTTP navigation command into a ROS2 goal and executes autonomous navigation using the Nav2 stack.
\end{enumerate}


\section{Exterior Design of the Robot}
\subsection{}


\section{Electrical System Design}
\subsection{Power Supply Architecture}
\begin{figure}[h]
\begin{center}
    \includegraphics[width=10cm,height=7.7cm]{powerdiagram.png}
\end{center}

\caption{Overview of Power Supply Architecture}
\label{fig:3.1}
\end{figure}
\subsection{Power Consumption Analysis}

\subsection{}

\section{Exterior Design of the Robot}
\subsection{fgsdx}

\subsection{Power Consumption Analysis}

\subsection{}


\section{Software Design and Development}

\subsection{Software Architecture Overview}

The system is structured into three tightly integrated layers: (1) a vector database for semantic retrieval, (2) a relational database for structured data management, and (3) an LLM integration module for conversational intelligence. These components work together to support real-time interaction, contextual reasoning, and personalised responses.

The five software repositories that constitute the system are assigned to their respective hardware nodes as follows.

\begin{itemize}
    \item \textbf{JetsonVision} -- Python application deployed on the NVIDIA Jetson Orin Nano. Responsible for real-time face detection, multi-face tracking, head pose estimation, and face recognition.

    \item \textbf{RaspiReceive} -- Python asyncio application deployed on Pi~5 \#1. Responsible for microphone capture, voice activity detection, speech transcription, detection-speech correlation, and audio playback.

    \item \textbf{baymax-face-ui} -- Electron.js frontend combined with a Python FastAPI backend, deployed on Pi~5 \#1. Renders the robot's emotional face on an HDMI display.

    \item \textbf{capstone\_server} -- Python FastAPI application deployed on the AI Brain server. Hosts the complete AI pipeline including grammar correction, RAG retrieval, LLM inference, TTS synthesis, and response dispatch.

    \item \textbf{Ros2\_robot} -- ROS2 Jazzy package suite deployed on Pi~5 \#2. Provides hardware bring-up, sensor drivers, AMCL localisation, Nav2 path planning, and an HTTP bridge for external command injection.
\end{itemize}

\subsection{Database Design}

A hybrid database architecture is adopted to leverage the strengths of both vector and relational storage. The vector database enables efficient semantic search for unstructured knowledge, while the relational database ensures data integrity and supports transactional operations.

\subsubsection{Milvus Vector Database}

Milvus serves as the semantic memory layer, storing embeddings of unstructured data such as university information, curriculum content, and timetable data. Instead of raw text storage, documents are encoded into dense vectors using the BAAI/bge-m3 model (1024 dimensions), enabling fast similarity search. An \textbf{IVF\_FLAT} index is constructed for each collection with \textbf{nlist = 128} and a query-time parameter \textbf{nprobe = 10}, enabling approximate nearest-neighbour retrieval with cosine similarity as the distance metric.

The database is organised into four primary collections:

\begin{itemize}
    \item \textbf{uni\_info:} Contains seven entries describing campus buildings, facilities, zones, and departmental information within Building E-12 at KMITL. Serves as the default fallback retrieval source for campus navigation queries.

    \item \textbf{curriculum:} Derived from the Robotics and Artificial Intelligence Engineering programme specification document, containing 516 Thai-language chunk embeddings covering course descriptions, learning outcomes, prerequisites, and credit structures for Years 1 through 4.

    \item \textbf{time\_table:} Constructed from an academic Excel timetable dataset. Each entry encodes a structured Thai sentence of the form (day, time slot, course name, room) to maximise retrieval precision, resulting in 128 entries.

    \item \textbf{conversation\_memory:} A dynamic collection storing per-student LLM-generated conversation summaries, indexed by \textbf{student\_id}. Supports filtered semantic retrieval, allowing the system to recall contextually relevant past interactions for a specific student during personalised prompt construction.
\end{itemize}

Semantic retrieval is performed using approximate nearest-neighbour search with an IVF\_FLAT index and cosine similarity, significantly reducing response latency compared to traditional search methods.

\subsubsection{MySQL Relational Database}

MySQL manages structured data required for system operations. The schema enforces referential integrity and supports efficient querying of user-related information.

Key tables include:

\begin{itemize}
    \item \textbf{Academic\_Year:} Stores academic calendar information, including year boundaries used for tone adaptation in personalised prompts.

    \item \textbf{Students:} Maintains student identity records, including student identifier, full name in Thai and English, nickname, academic year, and enrolment status. This table is the authoritative source for student metadata injected into LLM prompts.

    \item \textbf{Face\_Recognition\_Data:} Stores biometric face encodings linked to student identities. Used by the Jetson Orin Nano enrolment tool to maintain the face embedding database.

    \item \textbf{ExcelTimetableData:} Contains structured timetable records for precise SQL-based schedule lookups, complementing the semantic retrieval provided by the \textbf{time\_table} Milvus collection.
\end{itemize}

\subsubsection{Database Synchronisation}

Timetable data is synchronised across both systems: MySQL stores structured records, while Milvus stores corresponding embeddings for semantic retrieval. Updates are applied to both layers to maintain consistency between structured queries and conversational responses.

The \textbf{conversation\_memory} collection is updated asynchronously at the end of each conversation session. The 8B LLM summarises the session transcript into a concise natural-language paragraph, which is then embedded and stored in Milvus tagged with the student's \textbf{student\_id}. This fire-and-forget pattern prevents memory writes from blocking the main response pipeline.

\subsection{LLM Integration Module}

The LLM module serves as the conversational core, implementing a multi-route RAG pipeline that dynamically selects the appropriate data source for each query. The system uses a dual-model setup: a high-capacity 70B model for response generation and a lightweight 8B model for preprocessing tasks such as grammar correction and memory summarisation. Both models run via the Ollama inference server with GPU acceleration.

\begin{itemize}
    \item \textbf{Main chatbot:} \textbf{llama3.1-typhoon2-70b-instruct} (Q5\_K\_M GGUF) -- used for greeting generation and final response synthesis.

    \item \textbf{Fast assistant:} \textbf{llama3.1-typhoon2-8b-instruct} -- used for grammar correction and end-of-session memory summarisation.

    \item \textbf{Audio sidecar:} \textbf{typhoon-ai/llama3.1-typhoon2-audio-8b-instruct} -- a multimodal model deployed as an independent sidecar process on port 8001, handling both STT transcription and TTS synthesis.
\end{itemize}

\subsubsection{Query Routing Strategy}

User queries are classified using keyword-based intent detection and routed to one of six data sources:

\begin{itemize}
    \item \textbf{Conversation Memory:} Retrieves the top-3 most semantically similar past conversation summaries from the \textbf{conversation\_memory} Milvus collection, filtered by \textbf{student\_id}. Provides long-term personalisation context.

    \item \textbf{Student Information:} Executes a structured SQL query against MySQL to retrieve the student's academic year and metadata. The \textbf{student\_year} field defaults to 1 on failure to prevent pipeline interruption.

    \item \textbf{Timetable:} Activated when keywords associated with scheduling are detected in the query. Retrieves top-3 embeddings from \textbf{time\_table} and optionally augments with a MySQL lookup for precise room and time data.

    \item \textbf{Curriculum:} Activated when course-related keywords are present. Performs semantic search over the \textbf{curriculum} collection (top-3 chunks) to retrieve programme-specific information.

    \item \textbf{University Information:} Default fallback channel. Retrieves from \textbf{uni\_info} when no other channel matches, covering campus layout, building zones, and general facilities.

    \item \textbf{General Chat:} Handles open-ended conversational queries without external retrieval. The LLM generates responses solely from its parametric knowledge and the current session history.
\end{itemize}

\subsubsection{Personalised Prompt Generation}

Responses are dynamically tailored using student metadata, including name and academic year. Tone adaptation is applied based on student level, ensuring appropriate communication style. Prompts also include recent conversation summaries and retrieved contextual knowledge to maintain coherence.

The system prompt specifies the robot's persona as a female Thai-language assistant with the following behavioural constraints:

\begin{itemize}
    \item All speech particles must be feminine; masculine particles and first-person masculine pronouns are prohibited.

    \item The student is addressed by nickname only; full names are never used mid-conversation.

    \item Responses are capped at two sentences to ensure TTS output remains concise and intelligible.

    \item Numbers, acronyms, and building codes are expanded into their full Thai phonetic representations to ensure correct TTS pronunciation (e.g., E-12 is rendered as its Thai phonetic equivalent; room numbers are spoken digit-by-digit).
\end{itemize}

\subsubsection{Output Schema and Intent Classification}

The LLM returns structured JSON outputs containing response text and intent classification. Supported intents include:

\begin{itemize}
    \item \textbf{chat / info:} Triggers speech output only.
    \item \textbf{navigate:} Triggers speech and ROS2 navigation commands.
    \item \textbf{farewell:} Ends session and resumes autonomous roaming.
\end{itemize}

\subsubsection{Session Memory and Persistence}

The system uses a dual storage strategy:

\begin{itemize}
    \item \textbf{SQLite:} Stores full conversation logs for auditing and debugging.
    \item \textbf{Milvus:} Stores summarised embeddings for long-term semantic memory.
\end{itemize}

Session states are maintained in-memory with timeout policies (30 minutes for registered users, 5 minutes for guests). A silence-based timeout mechanism triggers reprompting and automatic session termination. A per-student greeting cooldown of 300 seconds is enforced by the \textbf{/greeting} endpoint to prevent repeated activation caused by transient face detection events within a single encounter.

\subsection{Speech-to-Text and Grammar Correction}

Speech processing is divided into two stages: transcription and linguistic refinement.

\subsubsection{ASR via Typhoon2-Audio}

Audio input is sent to the Typhoon2-Audio sidecar via a POST \textbf{/stt} request. The model processes 16~kHz PCM audio and returns Thai transcription with typical latency of 1--3 seconds.

The system supports two operational STT modes. Mode B is the recommended production configuration as it leverages GPU-accelerated inference on the server and eliminates local model loading on the Pi.

\begin{itemize}
    \item \textbf{Mode A (edge STT):} Pi~5 \#1 performs on-device transcription and sends the resulting text as a JSON payload to the server via \textbf{/detection}. Two backends are available under Mode A. The recommended backend is \textbf{scb10x/typhoon-asr-realtime} via the NVIDIA NeMo Toolkit (\textbf{--stt-backend typhoon-local}), achieving 0.68--2.0 seconds per segment on Pi~5 CPU with no hallucinations observed in live testing. The fallback backend is \textbf{biodatlab/distill-whisper-th-small} via faster-whisper INT8 quantisation (\textbf{--stt-backend whisper}), achieving 8.5--9.7 seconds per segment and requiring hallucination mitigations including VAD filtering, average log-probability thresholding, and a \textbf{max\_new\_tokens} cap of 50.

    \item \textbf{Mode B (server STT):} Pi~5 \#1 forwards raw PCM WAV audio as a multipart payload to \textbf{/audio\_detection}. The server invokes the Typhoon2-Audio-8B sidecar for GPU-accelerated transcription before the remaining inference pipeline proceeds. This mode requires no local STT model on the Pi, enabling startup in approximately 3 seconds.
\end{itemize}

\begin{table}[h]
\centering
\label{tab:stt_comparison}
\begin{tabular}{|l|l|l|l|}
\hline
\textbf{Backend} & \textbf{Speed (Pi~5 CPU)} & \textbf{RAM} & \textbf{Role} \\ \hline
\textbf{scb10x/typhoon-asr-realtime} (NeMo) & 0.68--2.0 s/seg & $\sim$2.8 GB + 1.7 GB swap & Mode A primary \\ \hline
\textbf{biodatlab/distill-whisper-th-small} (faster-whisper INT8) & 8.5--9.7 s/seg & $\sim$800 MB--1 GB & Mode A fallback \\ \hline
Typhoon2-Audio-8B (server GPU)              & 1--5 s/seg      & GPU (server)    & Mode B (recommended) \\ \hline
\end{tabular}
\caption{STT Backend Comparison for Edge Audio Coordination Subsystem}
\end{table}

\subsubsection{Grammar Normalisation}

The 8B LLM corrects spelling and phonetic errors while preserving meaning. Strict constraints ensure that no additional content is introduced. Grammar correction is bypassed when the STT confidence score exceeds a threshold of 0.85, avoiding unnecessary LLM invocation for high-confidence transcriptions.

\subsubsection{Robustness Mechanisms}

Several safeguards are implemented:

\begin{itemize}
    \item Minimum input length filtering -- transcriptions shorter than three characters are rejected by a noise gate and do not trigger a server inference call.

    \item Output length validation (50--150\%) -- the grammar-corrected output must fall within 50--150\% of the input character length. Outputs outside this range trigger reversion to the original raw transcription.

    \item Timeout fallback to raw transcription -- if the grammar correction LLM call exceeds a configurable timeout, the raw transcription is used without correction to maintain conversational responsiveness.
\end{itemize}

\subsection{Text-to-Speech Module}

The TTS subsystem converts responses into spoken Thai using a preprocessing and synthesis pipeline. It handles Thai phonetic structures and converts non-Thai elements into readable forms.

Text is sent to the Typhoon2-Audio sidecar via \textbf{/tts}, where it is synthesised into 16~kHz PCM WAV audio. The output is then forwarded to Raspberry Pi~5 for playback. The sidecar is preferred due to its faster GPU-based inference compared to edge-only processing.

\subsubsection{Text Normalisation}

Preprocessing includes:

\begin{itemize}
    \item English and acronym expansion.
    \item Number-to-Thai conversion.
    \item Unicode normalisation.
    \item Word and syllable segmentation.
\end{itemize}

\subsubsection{Asynchronous Dispatch}

TTS and navigation commands are executed asynchronously using \textbf{asyncio}, ensuring the main pipeline remains non-blocking and responsive. This pattern eliminates the 15--25 second blocking observed when TTS was awaited synchronously on slow Raspberry Pi~5 networks, freeing the server to accept the next student interaction immediately.

\subsection{Inference Pipeline}

Each \textbf{/detection} request follows a sequential pipeline:

\begin{enumerate}
    \item Input filtering (noise gate).
    \item Grammar correction.
    \item Query routing and context retrieval.
    \item Memory retrieval.
    \item LLM response generation.
    \item Asynchronous logging and memory update.
    \item Intent-based dispatch (TTS / navigation).
    \item System reactivation for next input.
\end{enumerate}

\subsection{Vision and Face Recognition Subsystem}

The vision subsystem (JetsonVision) is the first processing stage in the interaction pipeline. It operates continuously on the Jetson Orin Nano and is responsible for detecting, tracking, and identifying human faces in the robot's camera field of view.

\subsubsection{Detection and Tracking}

Face detection is performed using the SCRFD (Sample and Computation Redistribution Face Detector) model with a 640$\times$640 input resolution. The detector is configured with a confidence threshold of 0.5 and a Non-Maximum Suppression (NMS) IoU threshold of 0.4. The model is accelerated using TensorRT via the ONNX Runtime TensorRT execution provider when operating in GPU mode; a CPU-only ONNX fallback is available via the \textbf{--cpu-only} command-line flag.

SCRFD produces bounding boxes and five facial landmark points per detected face. These outputs are passed to a ByteTracker multi-object tracker, which assigns a persistent \textbf{track\_id} to each face across consecutive frames. ByteTracker maintains track continuity through brief occlusions and frames in which detection confidence drops below the threshold, providing stable identity anchors for downstream recognition.

\subsubsection{Head Pose Estimation and Frontal Gate}

A lightweight head pose estimator computes the yaw, pitch, and roll of each tracked face from the five SCRFD landmark points. Only faces satisfying the frontal constraint of yaw less than 25 degrees and pitch less than 15 degrees are eligible for recognition. This gate prevents recognition attempts on profile views, where ArcFace embedding quality degrades significantly, thereby reducing false-positive matches.

\subsubsection{Face Recognition}

Eligible face crops are passed to an ArcFace model with a ResNet backbone that produces 512-dimensional face embeddings. Inference is performed in FP16 precision to utilise the Tensor Cores available on the Jetson Orin Nano's GPU. A cooldown gate of 5 seconds per \textbf{track\_id} ensures that repeated recognition calls for the same continuously-present face do not saturate the system.

Recognition is performed by computing the cosine similarity between the query embedding and each stored embedding in \textbf{data/enrollments.json}. If the maximum similarity across all enrolment entries for a given identity exceeds a configurable threshold, the face is labelled as a known student and the associated metadata is returned; otherwise, the face is labelled unknown and assigned a temporary UUID as its \textbf{person\_id}.

Student enrolment is performed offline using the \textbf{enroll\_student.py} tool, which guides the operator to capture five face angles (frontal, left, right, and two 45-degree intermediate poses) and stores the corresponding embeddings alongside the student's Thai and English full names and nickname.

\subsubsection{WebSocket Event Emission}

Upon a successful recognition or unknown-face detection event, the Jetson transmits a JSON message to Pi~5 \#1 over the persistent WebSocket connection on port 8765. The message schema carries a top-level \textbf{status} field set to \textbf{detected}, and a nested \textbf{metadata} object containing the fields \textbf{eng\_name}, \textbf{thai\_name}, \textbf{student\_id}, \textbf{is\_registered}, and \textbf{confidence}. For unrecognised faces, \textbf{is\_registered} is set to false and the name fields are null.

On the receiving side, the Pi~5 VisionReceiver applies a confidence threshold of 0.75 before forwarding detection events to the coordinator. A 15-second debounce is applied per face, ensuring that the detection callback fires at most once every 15 seconds for a continuously present individual, preventing the asyncio queue from being flooded during the STT processing window. Null identity fields are mapped to the string value \textbf{Unknown} before use in downstream payloads.

\subsection{Edge Audio Coordination Subsystem}

The edge audio coordination subsystem (RaspiReceive) deployed on Pi~5 \#1 serves as the interaction hub. It bridges the vision subsystem and the AI server by combining face detection events with speech transcription results into a unified payload, while managing audio capture and playback.

\subsubsection{Concurrent Task Architecture}

RaspiReceive is implemented as a single \textbf{asyncio} event loop running four concurrent coroutines via \textbf{asyncio.gather}:

\begin{itemize}
    \item \textbf{\_detection\_loop():} Listens on WebSocket port 8765 for incoming Jetson events, applies session upgrade logic from Unknown to Known, and enqueues events for the combiner.

    \item \textbf{\_stt\_consumer\_loop():} Drains the output queue of the AudioWorker background thread, accumulating transcription results in a rolling recent buffer.

    \item \textbf{\_combiner\_loop():} Executes every 500~ms, matching detection events with transcription results according to the gating rules described below, and dispatching combined payloads to the AI server.

    \item \textbf{\_run\_activation\_server():} Hosts the FastAPI HTTP service on port 8766, providing endpoints for audio playback, microphone activation control, and health monitoring.
\end{itemize}

A background AudioWorker thread, running outside the asyncio event loop, handles blocking microphone input/output via PyAudio at 16~kHz mono. Captured audio frames are accumulated in a three-second rolling circular buffer from which speech segments are extracted by the SileroVAD model. The AudioWorker is paused during TTS playback and resumed upon completion, preventing the robot from transcribing its own synthesised speech output.

The activation state is managed through a push-based model: the server posts \textbf{/set\_active} with \textbf{active = 1} to re-enable the microphone after each response, and \textbf{active = 0} to suppress new input during processing. An auto-reactivation timer of 60 seconds serves as a fallback in the event that the server call does not arrive.

\subsubsection{Voice Activity Detection and Transcription}

A two-tier VAD pipeline is employed to eliminate hallucinations caused by silent or low-energy ambient audio. In the first tier, a pre-emphasis filter boosts the amplitude of Thai tonal consonants before analysis. In the second tier, the SileroVAD model assigns a speech probability to each audio chunk; chunks with probability exceeding the threshold of 0.73 are classified as speech. Only frames passing both tiers are appended to the utterance accumulator. This two-tier approach eliminates the hallucination loops observed with single-stage VAD, where models such as faster-whisper would generate repeated Thai or English phrases on background noise.

Upon detecting the end of a speech segment, the accumulated audio is submitted to the active STT backend. The transcription result is wrapped in an \textbf{STTChunk} dataclass and placed on the output queue consumed by \textbf{\_stt\_consumer\_loop()}. The microphone input is automatically muted during TTS audio playback by pausing the AudioWorker, preventing the robot from transcribing its own synthesised speech. The AudioWorker resumes microphone capture only after playback has completed.

\subsubsection{Detection-Speech Combiner Logic}

The \textbf{\_combiner\_loop()} implements four sequential gating rules before dispatching a payload to the AI server:

\begin{enumerate}
    \item \textbf{Consecutive detection gate:} At least two detection events for the same \textbf{track\_id} must arrive within a ten-second sliding window before a send is triggered. This prevents false activations from brief, transient face detections.

    \item \textbf{Unknown-to-Known upgrade window:} When an unknown face is first detected, the combiner holds the event for up to ten seconds (\textbf{unknown\_hold\_sec = 10}) waiting for the same \textbf{track\_id} to receive a known identity from a subsequent Jetson recognition event.

    \item \textbf{STT wait:} After a valid detection event passes the consecutive gate, the combiner waits up to 60 seconds (\textbf{stt\_wait\_sec = 60}) for a non-empty transcription result. If the timeout expires, the payload is dispatched with an empty text field, triggering a contextual greeting on the server side.

    \item \textbf{Post-send cooldown:} After dispatching a payload, the combiner ignores new detection events for five seconds (\textbf{cooldown\_sec = 5}) to prevent duplicate triggers within the same interaction.
\end{enumerate}

\begin{table}[h]
\centering
\label{tab:raspi_config}
\begin{tabular}{|l|l|l|}
\hline
\textbf{Parameter} & \textbf{Default} & \textbf{Description} \\ \hline
\textbf{websocket\_port}                    & 8765  & Jetson detection listener port \\ \hline
\textbf{audio\_vad\_threshold}              & 0.73  & SileroVAD speech probability gate \\ \hline
\textbf{stt\_model\_name}                   & distill-whisper-th-small & Whisper model ID \\ \hline
\textbf{cooldown\_sec}                      & 5.0   & Post-send detection cooldown (s) \\ \hline
\textbf{unknown\_hold\_sec}                 & 10.0  & Unknown-to-Known upgrade window (s) \\ \hline
\textbf{stt\_wait\_sec}                     & 60.0  & Maximum wait for STT result (s) \\ \hline
\textbf{consecutive\_detections\_required}  & 2     & Detections needed within window \\ \hline
\textbf{activate\_timeout\_sec}             & 60.0  & Auto-reactivate if server silent (s) \\ \hline
\textbf{max\_tts\_duration\_sec}            & 30.0  & Playback watchdog timeout (s) \\ \hline
\end{tabular}
\caption{RaspiReceive Subsystem Configuration Parameters}
\end{table}

\subsubsection{Audio Playback and Watchdog}

Synthesised audio (WAV) received at the \textbf{/audio\_play} endpoint is queued for sequential playback via the system's \textbf{aplay} command. A background watchdog task monitors each aplay subprocess and force-terminates it if playback has not completed within 30 seconds (\textbf{max\_tts\_duration\_sec = 30}). This safeguard prevents the audio pipeline from entering a blocked state due to network transfer errors or corrupted WAV data.

\subsection{Expressive Robot Interface Subsystem}

The expressive interface subsystem (baymax-face-ui) renders an animated robot face on the HDMI display connected to Pi~5 \#1. The system communicates the robot's internal state to the interacting student through five discrete emotional expressions that are mapped to interaction phases.

\subsubsection{Expression System}

The face is rendered on an HTML5 Canvas element within a fullscreen Electron.js BrowserWindow. Animation is driven by a requestAnimationFrame loop with interpolated transitions between expression states. Table~3.4 describes the five supported expressions.

\begin{table}[h]
\centering
\label{tab:emotions}
\begin{tabular}{|l|l|l|l|}
\hline
\textbf{Code} & \textbf{Name} & \textbf{Visual Description} & \textbf{Triggered By} \\ \hline
0 & Idle     & Neutral with slow breathing animation    & Playback ends (chat / info / farewell) \\ \hline
1 & Scanning & Eyes sweep left-right                    & Navigating to a destination \\ \hline
2 & Happy    & Wide smile, sparkle particles            & Person detected, greeting about to fire \\ \hline
3 & Talking  & Mouth animates vowel shapes              & TTS audio playback in progress \\ \hline
4 & Thinking & Drooped eyelid, drifting iris            & STT ready, awaiting LLM response \\ \hline
\end{tabular}
\caption{Robot Face Expression States}
\end{table}

\subsubsection{IPC Architecture}

All face emotion transitions are managed locally by Pi~5 \#1. The server does not call the face service directly. Instead, the \textbf{raspi\_main.py} coordinator drives all transitions based on its own visibility into the interaction lifecycle: it sets \textbf{happy (2)} as soon as a person is identified and before posting to \textbf{/greeting}; it sets \textbf{thinking (4)} when the STT result is ready and the POST to the server is about to be dispatched; it sets \textbf{talking (3)} when audio playback begins; and it sets \textbf{idle (0)} when playback ends. For navigation intents, the transition after playback ends is \textbf{scanning (1)} rather than \textbf{idle (0)}, persisting until the next interaction.

Face calls are issued as fire-and-forget HTTP requests so that a slow or unresponsive face service never blocks the audio delivery pipeline. A Python FastAPI service (\textbf{pi5\_face\_service.py}) listens on port 7000 and accepts HTTP POST requests at the \textbf{/face\_emotion} endpoint. The request body specifies the target emotion code as a JSON integer field. The FastAPI process forwards emotion codes to the Electron renderer process via an internal WebSocket channel on \textbf{localhost:8768}, which acts as an IPC bridge. The Electron main process listens on this channel and invokes the appropriate canvas state transition in the renderer. This two-process architecture decouples the HTTP-facing service from the rendering engine, enabling independent restart of either component without disrupting the other.

\subsection{Autonomous Navigation Subsystem}

The navigation subsystem (Ros2\_robot) deployed on Pi~5 \#2 provides autonomous mobile navigation within the E12 Building floor at KMITL. It integrates hardware drivers, a LiDAR-based localisation stack, and a path planning framework, exposed to external control via an HTTP bridge.

\subsubsection{Hardware Layer and Odometry}

The robot chassis is modelled in URDF via the \textbf{ired.urdf.xacro} file, which defines the \textbf{base\_link}, \textbf{base\_footprint}, left and right drive wheel joints, IMU frame, and RPLiDAR mounting point. The iREDCr differential drive controller communicates with the wheel motors over a serial interface at \textbf{/dev/ttyiREDCr} and publishes wheel odometry to the \textbf{/odom\_encoder} topic.

A supplementary odometry source is provided by a scan-to-scan laser scan matcher (ros2\_laser\_scan\_matcher), which publishes its estimate to the \textbf{/odom} topic. The scan matcher is configured with keyframe thresholds of 0.05~m and 0.087~rad (approximately 5 degrees), triggering a new scan match only when the robot has moved or rotated beyond these limits. This laser-derived odometry exhibits lower drift under wheel slippage conditions and is used as the primary input to the AMCL particle filter.

\subsubsection{Localisation and Path Planning}

Localisation is performed by Adaptive Monte Carlo Localisation (AMCL) against a pre-built 2D occupancy grid map of the operating environment. The particle filter is seeded from an initial pose estimate; when no initial pose is available at startup, the robot executes a 360-degree in-place rotation to gather sufficient scan data for the filter to converge.

Path planning and execution are managed by the Nav2 stack. The global planner generates a collision-free path from the current estimated pose to the goal pose, and the DWB (Dynamic Window B) local controller tracks this path. The DWB controller is configured with a maximum translational velocity of 0.3~m/s and a stopped-velocity threshold of 0.25~m/s. The stopped-velocity threshold must be set strictly less than the maximum velocity to prevent the controller from interpreting normal cruising speed as a stopped condition, which was found during development testing to cause an indefinite in-place spinning behaviour.

\subsubsection{HTTP Bridge and Navigation State Machine}

An HTTP bridge service (\textbf{pi5\_service.py}), deployed as a standalone Python FastAPI application on port 8767, translates external HTTP commands from the AI Brain server into ROS2 navigation goals. The bridge exposes a \textbf{/nav\_state} endpoint that accepts a JSON body containing the fields \textbf{state} (integer 0, 1, or 2) and \textbf{destination} (room name string).

\begin{table}[h]
\centering
\label{tab:nav_states}
\begin{tabular}{|l|l|l|}
\hline
\textbf{State} & \textbf{Label} & \textbf{Action} \\ \hline
0 & Stop     & Cancel active navigation goal; robot halts in place \\ \hline
1 & Roam     & Cycle through patrol waypoints continuously \\ \hline
2 & Navigate & Execute goal-directed navigation to a named room \\ \hline
\end{tabular}
\caption{Navigation State Definitions}
\end{table}

Destination room names are resolved to metric goal poses (x, y, yaw in the map frame) via a static mapping defined in \textbf{goals.yaml}. The room-to-codename mapping is as follows: \textbf{e12-1} maps to label A, \textbf{e12-2} maps to label B, and \textbf{e12-3} maps to label C. Goal poses are determined experimentally by driving the robot to each target location and recording the AMCL pose estimate from the \textbf{/amcl\_pose} topic.

The \textbf{navigation\_manager.py} ROS2 node wraps the Nav2 BasicNavigator API and publishes the current navigation status to the \textbf{/navigation\_status} topic with values IDLE, ROAMING, NAVIGATING, SUCCEEDED, or FAILED. The node implements a stuck-detection mechanism: if the robot's displacement over a 15-second window is less than 0.05~m, it is considered stuck, and the node commands a 360-degree in-place rotation to help AMCL re-localise before retrying the goal. A maximum of two retries is permitted; if both fail, the status is published as FAILED and the state machine returns to IDLE.

\subsection{Inter-subsystem Communication Architecture}

The protocol selection for each communication link is motivated by the traffic characteristics of that link.

\begin{table}[h]
\centering
\label{tab:protocols}
\begin{tabular}{|l|l|l|}
\hline
\textbf{Link} & \textbf{Protocol} & \textbf{Rationale} \\ \hline
Jetson to Pi~5 \#1        & WebSocket (persistent) & Continuous event stream; avoids TCP handshake overhead \\ \hline
Pi~5 \#1 to Server        & HTTP POST              & Request-response matches trigger-and-wait pattern \\ \hline
Server to Pi~5 \#1 audio  & HTTP POST              & One-shot delivery of binary WAV payload \\ \hline
Server to Pi~5 \#1 face   & HTTP POST              & Fire-and-forget display state command \\ \hline
Server to Pi~5 \#2        & HTTP POST              & Decouples AI server from ROS2 middleware \\ \hline
Pi~5 \#2 bridge to Nav    & ROS2 topic (std\_msgs/String) & Standard ROS2 IPC within single device \\ \hline
\end{tabular}
\caption{Communication Links and Protocol Selection Rationale}
\end{table}

\subsection{System Data Flow and Interaction Scenarios}

\subsubsection{Scenario A: Registered Student -- Conversational Query}

\begin{enumerate}
    \item The Jetson detects a frontal face and extracts a 512-dimensional ArcFace embedding. Cosine similarity search over the enrolment database yields a match, and a WebSocket detection event is transmitted to Pi~5 \#1 with \textbf{is\_known} set to true.

    \item The student speaks; SileroVAD detects voice activity above the 0.73 threshold and the Whisper model produces the transcription.

    \item The combiner confirms two detection events within the ten-second window, assembles the combined payload, and dispatches it via POST to \textbf{/detection}.

    \item The server applies grammar correction (bypassed if confidence exceeds 0.85), classifies the query intent, and routes to the appropriate RAG channel.

    \item Milvus returns the top-3 semantically similar knowledge entries. The 70B LLM generates a two-sentence Thai response and classifies the intent as \textbf{chat} or \textbf{info}.

    \item The Typhoon2-Audio sidecar synthesises the response into a WAV file (approximately 105~KB, approximately 633~ms synthesis time).

    \item The server concurrently dispatches: (a) WAV to \textbf{/audio\_play}, (b) active = 0 to \textbf{/set\_active} to mute the microphone during playback.

    \item Pi~5 \#1 receives the WAV, sets emotion code 3 (Talking) on the local face display, and plays the audio through the speaker via \textbf{aplay}.

    \item Upon playback completion, Pi~5 \#1 sets emotion code 0 (Idle). The server then sends active = 1 to \textbf{/set\_active}, re-enabling the microphone for the next utterance.
\end{enumerate}

\subsubsection{Scenario B: Navigation Request}

Steps 1 through 5 are identical to Scenario A, with the LLM classifying the intent as \textbf{navigate} and identifying the destination room.

\begin{enumerate}
    \setcounter{enumi}{5}
    \item The server dispatches: (a) synthesised WAV of the verbal confirmation to \textbf{/audio\_play}, (b) navigation command with state = 2 and the destination room name to \textbf{/nav\_state} on Pi~5 \#2. Pi~5 \#1 sets emotion 3 (Talking) when playback begins, then emotion 1 (Scanning) when playback ends, reflecting the robot's transition into navigation mode.

    \item Pi~5 \#2 receives the command, resolves the room name to the corresponding goal pose, and invokes BasicNavigator.goToPose().

    \item Nav2 plans a path using the AMCL-estimated current pose and the DWB controller executes trajectory following at up to 0.3~m/s.

    \item Upon reaching the goal, navigation\_manager publishes SUCCEEDED on \textbf{/navigation\_status} and transitions the state machine to IDLE.
\end{enumerate}

\subsection{Session and State Management}

\subsubsection{Robot Interaction State}

From the perspective of the RaspiReceive coordinator, the robot occupies one of two interaction states at any given time:

\begin{itemize}
    \item \textbf{INACTIVE:} The microphone is disabled. The robot is either in standby or in playback mode. New detection events are queued but not acted upon.

    \item \textbf{ACTIVE:} The microphone is enabled and the combiner loop is actively matching detection events with speech input. The transition from INACTIVE to ACTIVE occurs when the server sends \textbf{set\_active = 1}, either upon TTS playback completion or after the \textbf{activate\_timeout\_sec} (60~s) timer fires.
\end{itemize}

\subsubsection{Student Session Lifecycle}

On the AI Brain server, each interaction is associated with a session object identified by the \textbf{person\_id} field of the incoming payload. The session lifecycle proceeds as follows:

\begin{enumerate}
    \item \textbf{Session creation:} A new session is created on the first detection event for a given \textbf{person\_id}. The session stores the student's metadata, initialises an empty short-term history buffer, and loads the top-3 relevant long-term memory summaries from Milvus.

    \item \textbf{Session continuation:} Subsequent detection events within the timeout window extend the session. Short-term history is updated after each exchange.

    \item \textbf{Session expiry and persistence:} If no detection event is received within the timeout period (30 minutes for registered students, 5 minutes for guests), the session is closed. For registered students, the 8B LLM summarises the session transcript and the resulting embedding is stored in the \textbf{conversation\_memory} Milvus collection.
\end{enumerate}

\subsubsection{Navigation State Machine}

The navigation state machine on Pi~5 \#2 transitions between the following states:

\begin{itemize}
    \item \textbf{IDLE:} No active goal. The robot is stationary and awaiting the next command from the HTTP bridge.

    \item \textbf{ROAMING:} The robot is cycling through pre-defined patrol waypoints. Transitions to IDLE on receipt of state = 0.

    \item \textbf{NAVIGATING:} The robot is actively pursuing a specific goal pose. Transitions to SUCCEEDED on goal completion, to IDLE on state = 0, or enters the stuck-recovery sub-sequence if no progress is detected for 15 seconds.

    \item \textbf{SUCCEEDED:} The goal has been reached. Automatically transitions back to IDLE after publishing the status.

    \item \textbf{FAILED:} Two consecutive navigation attempts have failed. The state machine resets to IDLE and logs the failure for monitoring.
\end{itemize}

